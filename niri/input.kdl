input {
    keyboard {
        xkb {
            options "caps:escape"
        }
        numlock
    }
    touchpad {
        tap
        natural-scroll
    }

    warp-mouse-to-focus
    focus-follows-mouse max-scroll-amount="50%"
}
